# 课程仓库协作规范

## 工作边界

- 课程编号固定为 `00`–`37`，不得合并或删除。
- `00`–`08` 为完整基础课；`09`–`37` 为可继续扩写的结构化课程骨架。
- 每节课程必须使用[课程写作规范](docs/课程写作规范.md)规定的 20 个固定栏目。
- 不创建应用、支付、云函数或生产业务代码，除非后续任务明确要求。

## 安全与合规

- 不写入、展示、提交或上传 AppID、支付密钥、证书、手机号、身份证件、真实个人数据或其他凭据。
- 凭据只能通过环境变量或受控配置注入；示例一律使用占位符。
- 涉及微信公众平台、支付、备案、审核或资质的变化性规则，必须原样标注：`执行时请以微信公众平台当前页面和官方文档为准。`
- 金额使用整数分；订单、支付、退款与核销等敏感状态仅允许后端变更。

## 修改与验证

- 只修改当前任务必需的文件，保持简体中文和现有目录结构。
- 新增或修改课程后，在仓库根目录运行 `node scripts/audit-course.mjs`。
- 审计通过前，不得宣称课程目录完整。
